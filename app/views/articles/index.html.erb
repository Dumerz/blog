<div class="jumbotron">
  <h1>Articles!</h1>
  <p>We talk about anything uder the sun, any topic which would interest your mind. This site includes the responsive CSS and HTML, so it also adapts to your viewport and device. Click New article to start your new article.</p>
  <p>
  <%= link_to 'New article', new_article_path, class: 'btn btn-lg btn-success', role: 'button'%>
  </p>
</div>
<div class="container">
  <div class="row">
  <% @articles.each do |article| %>
    <div class="col-md-4">
        <h3><%= link_to article.title, article_path(article) %></h3>
        <strong><%= article.created_at.to_date.to_formatted_s(:long) %></strong>
        <p><%= article.text %></p>
    </div>
  <% end %>
  </div>
</div>